# SeekDock-owned deployment overlay. DeepSeek Harness remains unmodified.
# Both agent backends share DSH's official model-provider directory. Pi changes
# reaction-loop orchestration only; it does not add a duplicate LLM route.
- insert:
    - id: agent-backend-pi
      name: "@seekdock/dsh-agent-backend-pi"

    - id: ui-agent-backend
      name: "@seekdock/dsh-client-ui-agent-backend"
